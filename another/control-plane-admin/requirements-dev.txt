-r requirements.txt

pytest>=8.3,<9
# mongomock эмулирует API pymongo в памяти — позволяет тестировать
# MongoUserRepository без поднятого MongoDB (ни реального, ни докер-контейнера).
mongomock>=4.1,<5
