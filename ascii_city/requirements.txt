# The hub already installs fastapi, uvicorn, motor and pymongo from the root
# requirements.txt into every worker venv. Versions are pinned to the same
# values so this project can never drag a neighbour's environment sideways.
fastapi==0.115.6
uvicorn[standard]==0.32.1
motor==3.6.0
pymongo==4.9.2
websockets
