# Ядро монолита.
#
# build.sh ставит плагины ПОСЛЕ корня и по алфавиту, поэтому последний пин
# побеждает. Версии ниже — это то, что реально остаётся установленным после
# полного прогона build.sh (жёстче всех пинит netlazy). Раньше здесь стояли
# fastapi==0.104.1 / motor==3.3.2 / pymongo==4.5.0, которые всё равно
# затирались netlazy — сборка каждый раз ставила и сносила лишние версии.
# Меняя что-то здесь, сверься с netlazy/requirements.txt.
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-dotenv==1.0.0
motor==3.6.0
jinja2==3.1.2
pydantic>=2.7
pymongo==4.9.2
httpx
python-multipart>=0.0.9
websockets
psutil
cloudinary==1.41.0
Pillow>=10.0.0
cryptography>=42.0.0
